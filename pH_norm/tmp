	if (isHolding(leftButton) && repeatInterval())
		g_left_holding = true;
	else
		g_left_holding = false;

bool repeatInterval()
{
	if (millis() - repeatMillis > REPEAT_TIME) {
		repeatMillis = millis();
		return true;
	}
	else
		return false;
}
